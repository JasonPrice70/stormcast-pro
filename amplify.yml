version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx vite build --mode production
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
